[phases.install]
cmds = ['npm install --prefix backend --legacy-peer-deps', 'npm install --prefix frontend --legacy-peer-deps']

[phases.build]
cmds = ['CI=false npm run build --prefix frontend']

[start]
cmd = 'node backend/src/index.js'
